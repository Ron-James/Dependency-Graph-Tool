# Extending Scene Dependency Graph with Specialized Nodes

The old `IDependencyEmitter` contract has been removed from this sample.

To extend graph behavior, add scanner and styling updates under the editor SceneDependencyGraph code.
